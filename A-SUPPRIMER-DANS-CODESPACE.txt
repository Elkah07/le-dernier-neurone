SUPPRIMER CES DEUX ANCIENS FICHIERS AVANT LA COMPILATION :

vite.config.ts
drizzle.config.ts

Ils appartiennent a l'ancien hebergement ChatGPT/Cloudflare et ne sont plus
utilises par la version Firebase.

Ensuite executer :

npm install
npm run build && firebase deploy --only hosting,database

Ne pas lancer npm audit fix --force.
